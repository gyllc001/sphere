FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
COPY apps/api/package*.json ./apps/api/
COPY packages/ ./packages/

RUN npm ci

COPY . .

WORKDIR /app/apps/api

CMD ["sh", "-c", "node dist/db/migrate.js && echo 'MIGRATE_OK' && node dist/index.js"]
