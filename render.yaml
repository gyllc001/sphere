services:
  - type: web
    name: sphere-api
    runtime: docker
    # Dockerfile lives at apps/api/Dockerfile but is built from the repo root
    # (it references packages/ and the workspace root package.json)
    dockerfilePath: apps/api/Dockerfile
    # Override CMD to run migrations before starting the server
    healthCheckPath: /health
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sphere-db
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8080
      - key: ALLOWED_ORIGINS
        value: https://sphere-web-lemon.vercel.app
      # Set these manually in the Render dashboard after first deploy:
      - key: JWT_SECRET
        generateValue: true
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ADMIN_API_KEY
        sync: false

databases:
  - name: sphere-db
    plan: free
    databaseName: sphere
    user: sphere
